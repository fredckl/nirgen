version: '3'

services:
  nirgen:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'

    ports:
      - 8080:80

    environment:
      - CHOKIDAR_USEPOLLING=true
